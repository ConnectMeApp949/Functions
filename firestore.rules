rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Block all direct client access
    // All data access goes through Cloud Functions which use Admin SDK
    // Admin SDK bypasses these rules entirely
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
